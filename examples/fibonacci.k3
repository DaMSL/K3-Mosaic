foreign print_integer:(int -> unit)

trigger start(n:int) {} = send(fibonacci, localhost:10000, n, 0, 1)

trigger fibonacci(n:int, a:int, b:int) {} = if n == 0 then send(sink, localhost:10000, b) else send(fibonacci, localhost:10000, n - 1, b, a + b)

trigger sink(n:int) {} = print(n)
