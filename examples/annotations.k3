declare events : {|(int,int)|} @ { 1->2; index(2) }

trigger on_event(a:int, b:int) {} = do {
  insert(events, (a,b))
}

role client {
  source s1: (int,int) = file(csv, "s1.csv")
  bind s1 -> on_event
}

default role client
